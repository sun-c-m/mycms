# CMS 内容管理系统项目介绍

## 一、项目概述

本项目是一个前后端分离的内容管理系统（CMS）。后端使用 Spring Boot 3、MyBatis-Plus、MySQL 和 Flyway，前端使用 Vue 3、TypeScript、Vite、Element Plus、Pinia。系统围绕用户、角色、权限和新闻内容构建，采用 RBAC（基于角色的访问控制）模型。

## 二、已实现功能

- 用户登录、登录态持久化、退出登录和失效跳转
- 用户分页查询、新增、编辑、删除
- 角色创建、编辑、删除及权限分配
- 权限菜单树查询与权限维护
- 根据后端返回的菜单树动态注册前端路由
- 新闻列表、关键词搜索、状态筛选
- 新闻新增、编辑、删除、详情查看
- 新闻审核、发布和发布时间记录
- 新闻富文本编辑与附件上传
- 统一后台布局：侧边栏、顶部用户区、面包屑、欢迎页和统计卡片
- Axios 统一请求头、错误码、401/403/500 异常处理
- Flyway 数据库版本迁移和初始化数据

## 三、主要实现过程

1. 数据库使用 Flyway 管理版本，建立用户、角色、权限、新闻和附件等表，并通过初始化脚本写入菜单与角色数据。
2. 后端按 Controller、Service、Mapper、Entity/DTO/VO 分层。登录时查询用户及权限，Service 将扁平权限记录按 `parentId` 分组并排序，组装为菜单树返回。
3. 前端登录成功后把用户信息和菜单树保存到 Pinia（并持久化），路由守卫检查 token；根据菜单路径映射组件并调用 `router.addRoute` 注册动态路由。
4. 管理页面通过 Axios 调用 REST API，表格负责分页和筛选，表单负责校验和提交，操作成功后刷新列表并给出统一提示。
5. Axios 响应拦截器集中处理业务错误和鉴权失效，避免每个页面重复编写异常逻辑。

## 四、面试重点

**动态权限如何实现？** 登录接口返回权限树；前端路由守卫读取权限树，将允许访问的路径映射到页面组件并动态添加路由，同时后端接口继续进行权限校验，形成前后端双重保护。

**菜单树如何构建？** 先过滤按钮权限，再用 `parentId` 建立父子映射，挂载 children，最后按 sort 排序并返回 parentId=0 的顶级节点，时间复杂度为 O(n)。

**为什么使用 Flyway？** 数据库结构变更通过带版本号的 SQL 自动执行，保证开发、测试和生产环境的结构一致且可追踪。

**如何保证接口一致性？** 统一使用 Result 包装 code、message、data；前端 Axios 拦截器统一解包和处理错误码，页面只关注业务数据。

**可优化方向？** 生产环境可增加 JWT 刷新机制、操作审计、Redis 缓存、上传文件对象存储、接口限流和更细粒度的数据权限。
